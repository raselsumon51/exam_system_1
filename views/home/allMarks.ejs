<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Exam Result</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">

  <div class="container mx-auto mt-10 px-4 py-6">
    <!-- Title -->
    <h1 class="text-3xl font-semibold text-center text-indigo-600 mb-8">Exam Result</h1>

    <!-- Table Section -->
    <section class="bg-white p-6 rounded-lg shadow-md">
      <h2 class="text-2xl font-semibold text-gray-800 mb-4">Exam Result Details</h2>
      
      <table class="min-w-full bg-white border border-gray-200">
        <thead>
          <tr>
            <th class="px-6 py-3 border-b text-left text-lg font-medium text-gray-700">Student Email</th>
            <th class="px-6 py-3 border-b text-left text-lg font-medium text-gray-700">Subject Name</th>
            <th class="px-6 py-3 border-b text-left text-lg font-medium text-gray-700">Exam Name</th>
            <th class="px-6 py-3 border-b text-left text-lg font-medium text-gray-700">Number of Questions</th>
            <th class="px-6 py-3 border-b text-left text-lg font-medium text-gray-700">Obtained Marks</th>
            <th class="px-6 py-3 border-b text-left text-lg font-medium text-gray-700">Percentage of Correctness</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td class="px-6 py-4 border-b text-gray-700"><%= result.userDetails[0].email %></td>
            <td class="px-6 py-4 border-b text-gray-700"><%= result.examDetails[0].subjectName %></td>
            <td class="px-6 py-4 border-b text-gray-700"><%= result.examDetails[0].examName %></td>
            <td class="px-6 py-4 border-b text-gray-700"><%= result.examDetails[0].numberOfQuestions %></td>
            <td class="px-6 py-4 border-b text-gray-700"><%= result.score %></td>
            <td class="px-6 py-4 border-b text-gray-700">
              <%= ((result.score / result.examDetails[0].numberOfQuestions) * 100).toFixed(0) %>%
            </td>
          </tr>
        </tbody>
      </table>
    </section>
  </div>

</body>
</html>














